Repair selectRate(rows,date) in solution.mjs. Inputs are rows with id/from/to and a date, all dates valid YYYY-MM-DD strings; to:null means open-ended. Return the original matching row only if exactly one row contains the date, using inclusive start AND end boundaries. No matches or multiple matching rows must return null. Do not mutate rows or clone the returned row. Exports stay unchanged. Only solution.mjs may be changed; create temporary self-checks outside the workspace if desired. Hidden independent acceptance runs afterward. Do not spawn agents or access directories outside the workspace except Node standard runtime.
