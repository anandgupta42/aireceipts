Repair normalizeUsage(raw) in solution.mjs. input_tokens includes cached_input_tokens, so ordinary input is their difference. cached_input_tokens and cache_creation_tokens default to0 only when absent. cache_creation_tokens is a separate extra category. Return {input,cacheRead,cacheCreation,output,total}, where total=input_tokens+output_tokens+cache_creation_tokens. Required input_tokens/output_tokens and any supplied counters must be nonnegative finite safe integers; null is not absent. Cached may not exceed input. Unsafe summed total must be rejected. Invalid/contradictory input returns null, never clamps. Do not mutate raw. Exports stay unchanged. Only solution.mjs may be changed; create temporary self-checks outside the workspace if desired. Hidden independent acceptance runs afterward. Do not spawn agents or access directories outside the workspace except Node standard runtime.
